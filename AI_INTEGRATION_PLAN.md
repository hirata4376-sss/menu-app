# AI統合計画 — 引き継ぎ書

作成日: 2026-08-11
作成元: Cowork（設計フェーズ）
実行先: Claude Code（実装フェーズ）

---

## 0. このドキュメントの読み方（Claude Codeへ）

このファイルは Cowork 側で行った現状調査と方針決定の記録です。
**実装を始める前に、必ず以下を読んでください。**

1. 本ファイル全体
2. `AGENTS.md` — このプロジェクトの Next.js は 16.2.9 で、学習データと API・規約が異なる可能性がある旨の警告あり。**コードを書く前に `node_modules/next/dist/docs/` の該当ガイドを読むこと**
3. `GUIDELINE.md` — プロジェクトの目的と進行状況
4. `src/app/page.tsx` — 実質的な本体（1,114行）

そのうえで、**フェーズごとに計画を提示し、ユーザーの承認（y/n）を得てから実装に入ってください。**

---

## 1. 目的

献立の一括登録における「手動往復」を解消する。

**現在の流れ（人間が中継している）**

```
レシピ画像をスクショ
  → アプリの「呪文」をコピー
  → ChatGPT等に画像＋呪文を貼る
  → 返ってきたテキストをコピー
  → アプリのテキストエリアに貼る
  → 「まとめて登録する！」
```

**目指す流れ**

```
アプリで画像を選択
  → 登録完了
```

---

## 2. 決定事項（ユーザー承認済み）

| 項目 | 決定 | 備考 |
|---|---|---|
| 出力形式 | **JSON構造化出力に変更** | モデル側で形式を保証させる。現行の正規表現パースは廃止または縮小 |
| 使用API | **Google Gemini** | 無料枠が最大。画像認識が強い |
| 作業順序 | **① RLS → ② AI機能** | セキュリティを先に処理する |
| ローカルLLM | **不採用** | Vercel/スマホから使えない・画像読取が弱い・秘匿性要求が低い、の3点で不適 |

---

## 3. 現状の技術構成（調査済み）

| 項目 | 内容 |
|---|---|
| フレームワーク | Next.js **16.2.9** / React 19.2.4 / TypeScript |
| DB | Supabase（`menus` テーブル） |
| デプロイ | Vercel |
| ソース | `src/app/page.tsx`（1,114行・`"use client"`）、`src/utils/supabase.ts` |
| 環境変数 | `NEXT_PUBLIC_SUPABASE_URL` / `NEXT_PUBLIC_SUPABASE_ANON_KEY` のみ |
| **APIルート** | **存在しない**（`src/app/api/` 自体が未作成） |
| 認証 | **なし**（ログイン機能を持たない） |

### 重要な前提

- 全処理がブラウザ側で完結している。**サーバー側の処理を書く土台が今はない**
- Supabase へのアクセスは anon key を使ってブラウザから直接行っている
- `.bat` ファイル（`開発サーバー起動.bat` / `GitHubに送る.bat` / `Vercelにデプロイ.bat`）でデプロイ運用している

### 関連するコード上の要素

| 名前 | 場所 | 役割 |
|---|---|---|
| `Menu` 型 | 6行目付近 | `{ id, name, ingredients: string[], memo?, created_at? }` |
| `SEASONING_PREFIX` | 26行目付近 | `"#"`。調味料マーカー |
| `SEASONINGS` | 29行目付近 | 組み込み調味料辞書（約70語） |
| `BASE_PROMPT` | 188行目付近 | 現行の「呪文」 |
| `MAX_PROMPT_SUGGESTIONS` | 186行目付近 | `200`。登録済み食材名をプロンプトに差し込む上限 |
| `handleBulkAdd` | 476行目付近 | テキストを正規表現でパースして Supabase に insert |
| 一括登録UI | 966行目付近 | 呪文表示＋コピーボタン＋貼り付けテキストエリア |

---

## 3.5. 着手前の準備（フェーズ1より先に行う）

### 復旧できる範囲の整理

| 対象 | 復旧可否 | 手段 |
|---|---|---|
| アプリのコード | ✅ 可 | Git（`origin` = `hirata4376-sss/menu-app` にpush済み） |
| Vercel本番環境 | ✅ 可 | 管理画面から過去のデプロイへロールバック |
| Supabaseのポリシー設定 | ⚠️ Gitの管理外 | 事前記録が必要 |
| `menus` テーブルのデータ | ❌ Gitでは戻らない | **ユーザー判断でバックアップ不要と決定**（データ量が少なく、消えても許容） |

### 実施すること

- [ ] **作業前コミット**：未コミットの変更（`.gitignore`、`git_log.txt`）を整理し、`AI統合前の安定版` としてコミット＋push
- [ ] **Supabaseの現状記録**：`menus` テーブルのRLSが現在ON/OFFか、ポリシーの有無を確認して記録（スクショ可）
- [ ] ~~menusテーブルのCSVエクスポート~~ → **不要**（ユーザー判断済み）

### コミット運用の改善

`GitHubに送る.bat` は**コミットメッセージが `"update"` にハードコードされている**（該当行: `git commit -m "update"`）。
このため履歴が `update` だらけになり、どこまで戻すべきか判断できない。

今回の作業では、**`.bat` を使わず Claude Code 経由でコミットする**か、`.bat` にメッセージを引数で渡せるよう改修すること。
フェーズごとに意味のあるメッセージで区切ること。

---

## 4. フェーズ1：RLS（行レベルセキュリティ）

### 現状のリスク

Supabase の anon key はブラウザに露出している（`NEXT_PUBLIC_` 接頭辞のため設計上そうなる）。
RLS が未設定の場合、URL と key を知られると第三者がデータを読み書きできる状態になり得る。

**確信度：中** — Supabase 管理画面の実際の設定を未確認のため。まず現状を確認すること。

### ⚠ 注意：単純に RLS を ON にするとアプリが壊れる

このアプリには**認証機能がない**。
RLS を有効化してポリシー未設定のままにすると、全アクセスが拒否され、献立の読み書きが一切できなくなる。

「SQL を数行貼るだけ」では終わらない。**方式の選択が必要。**

### 検討すべき方式（Claude Code が比較・提案すること）

| 方式 | 概要 | 想定される長所／短所 |
|---|---|---|
| A. Supabase 匿名認証 | Anonymous Sign-in を有効化し、ユーザーごとに行を分離 | 標準的。ただし夫婦間でのデータ共有をどう実現するか要検討 |
| B. メール認証を追加 | 夫婦2人だけのログインを作る | 最も堅い。ただし「ログイン不要で即使える」という当初方針と衝突（GUIDELINE.md 3章） |
| C. サーバー経由に移行 | DB アクセスを API ルートに寄せ、service role key をサーバー側に隠す | フェーズ2で API ルートを作るので相性が良い。改修量は大きい |

**このプロジェクトは「夫婦2人で共有する」ことが前提**（GUIDELINE.md 7章「夫婦間データ共有・クラウド化」）である点を踏まえて提案すること。
利用者が2人しかいないため、厳密な行分離より「関係者以外を弾く」ことが要件に近い可能性が高い。

### 完了条件

- `GUIDELINE.md` シーズン2の「守りの三柱：RLS の設定」にチェックが入る
- 本番URLで従来通り献立の登録・編集・削除・検索ができる

---

## 5. フェーズ2：AI機能の直接統合

### 5-1. 全体構成

```
[ブラウザ] 画像を選択
    ↓ POST（画像データ）
[Next.js API Route]  ← ここを新設（src/app/api/ 配下）
    ↓ Gemini API 呼び出し（APIキーはサーバー側の環境変数）
[Gemini] 画像を解析し JSON で返す
    ↓
[API Route] 検証してブラウザへ返す
    ↓
[ブラウザ] 確認画面に表示 → ユーザーが承認 → Supabase へ保存
```

### 5-2. 必須のセキュリティ要件

**Gemini の API キーを `NEXT_PUBLIC_` 接頭辞で定義してはならない。**
ブラウザに露出し、第三者に無制限に使われて課金が発生する。

- 環境変数名は `GEMINI_API_KEY`（接頭辞なし）
- `.env.local` に記載する（`.gitignore` の `.env*` で除外済みであることを確認済み）
- Vercel の管理画面にも同じ環境変数を登録する（これを忘れると本番だけ動かない）

### 5-2-b. 課金を発生させない設定（ユーザー方針：無料枠のみで運用）

**大原則：Google AI Studio のプロジェクトに支払い方法（クレジットカード）を登録しない。**

支払い情報が未登録なら、無料枠を超えた時点で HTTP 429 が返ってリクエストが拒否されるだけで、課金は発生しない。これが最も確実な防御。

**実装上の対応**

- [ ] 429 エラーを正常系として扱い、「本日の無料枠を使い切りました。明日また試すか、手動での貼り付け登録をご利用ください」等のメッセージを出す
- [ ] 429 のときに**無限リトライさせない**（無駄な試行を繰り返すだけ）
- [ ] 手動コピペUIは**削除せず残す**方針を推奨（無料枠を使い切った際の代替手段になるため）

**モデル選定時の注意**

一部の新しいモデルは、支払い情報未登録のプロジェクトではクォータが 0 に設定されており使えない。
**「支払い情報なしで使えるモデル」の中から選ぶこと。** 実装前に Google AI Studio で実際に疎通確認すること。

**補足**

将来的に有料へ移行する場合は、Google Cloud 側で課金上限（キャップ）を設定できる。2026年4月から上限強制が施行されている。ただし現時点の方針は「カードを登録しない」であり、この設定は不要。

### 5-3. JSON スキーマ（叩き台）

現行の `BASE_PROMPT` のルールを JSON に移し替えたもの。Claude Code が調整してよい。

```json
{
  "menus": [
    {
      "name": "豚肉の生姜焼き",
      "ingredients": [
        { "name": "豚肉",   "amount": "200g",   "isSeasoning": false },
        { "name": "玉ねぎ", "amount": "0.5個",  "isSeasoning": false },
        { "name": "醤油",   "amount": "大さじ1", "isSeasoning": true }
      ]
    }
  ]
}
```

**保持すべき既存ルール（`BASE_PROMPT` 由来。精度に効いているので捨てないこと）**

1. 食材名は原則漢字（「ぶたにく」→「豚肉」）
2. 単位は `g / ml / 個 / 本 / 枚 / パック` に揃える
3. 分数は使わず小数（「1/2個」→「0.5個」）
4. 数字と単位は半角
5. 調味料は `isSeasoning: true` で表現（従来の `#` 接頭辞の役割を JSON フィールドに移す）
6. 1要素に複数の食材を入れない（「ごま油またはサラダ油」は不可）

**既存の食材名リストの引き継ぎ**

現行UIは、登録済み食材名を最大200件（`MAX_PROMPT_SUGGESTIONS`）プロンプトに差し込んで表記ゆれを防いでいる。
**この仕組みは API 化後も必ず維持すること。** むしろ自動で最新リストを渡せるため精度は上がるはず。

### 5-4. 実装タスク

- [ ] `GEMINI_API_KEY` を取得（Google AI Studio）し `.env.local` に設定
- [ ] `src/app/api/` 配下に画像解析用のルートを新設
  - **Next.js 16 の Route Handler の書き方を `node_modules/next/dist/docs/` で確認してから書くこと**
- [ ] Gemini の構造化出力（JSON スキーマ指定）で画像を解析する処理を実装
- [ ] `page.tsx` に**画像アップロードUI を新設**（現状は画像を扱うUIが一切ない。ファイル選択＋スマホのカメラ/写真からの入力を想定）
- [ ] 解析結果を**確認・修正してから登録する画面**を追加（後述の理由により必須）
- [ ] 承認後に Supabase へ insert（既存の保存処理を再利用）
- [ ] 既存の手動コピペUIの扱いを決める（残す／削除する）
- [ ] エラー処理：APIエラー、無料枠超過、画像が読めない、JSONが不正
- [ ] ローディング表示（Gemini の応答には数秒かかる）

### 5-5. 設計上の注意

**確認画面を必ず挟むこと。**
AI の読み取りは必ず誤る。現行は人間が目視してから貼り付けるため誤りに気づけるが、全自動にするとゴミデータが直接DBに入る。「解析結果を一覧表示 → ユーザーが修正 → 登録」の一段を残すこと。

**画像サイズの上限を設ける。**
スマホの写真をそのまま送ると重い。リサイズまたはサイズ上限のチェックを入れる。

**`page.tsx` は既に1,114行ある。**
これ以上の肥大化を避けるため、AI関連のロジックは別ファイルへの切り出しを検討すること。ただし**大規模なリファクタリングは今回のスコープ外**。勝手に全体を作り直さないこと。

---

## 6. 検証項目（各フェーズ完了時）

- [ ] `npm run build` が通る（型エラーなし）
- [ ] `npm run lint` が通る
- [ ] ローカル（`開発サーバー起動.bat`）で一連の動作を確認
- [ ] 既存機能が壊れていない（1件ずつ登録／一覧／編集／削除／食材逆引き検索）
- [ ] スマホの画面幅で崩れていない（モバイルファーストが方針）
- [ ] 本番デプロイ後、実機のスマホで確認
- [ ] Vercel に環境変数が設定済みか確認

---

## 7. スコープ外（今回やらないこと）

- ローカルLLMの導入
- 料理工程（レシピ手順）の追記機能
- ランダム1週間献立ルーレット
- 買い物リスト自動生成
- `page.tsx` 全体の大規模リファクタリング

---

## 8. 完了後にやること

- `GUIDELINE.md` の進行ステップを更新（シーズン3として追記）
- 本ファイルの決定事項に変更があれば追記する
- Vault のルール上、運用方法を変えたら定位置ドキュメントへ反映すること
