# 🍳 プロジェクト指針：献立アプリ（残り物レシピ管理）

## 1. 目的・ターゲット
- **目的**: 冷蔵庫の食材を使い切る。1週間分の献立を考える手間を削減する。
- **ターゲット**: 自分と妻（夫婦）。

## 2. 利用シーン
- 休日のまとめ買い前に、スマホのブラウザからサクッと操作できること。

## 3. MVP（最小限の実用構成）の範囲
- **献立の登録**: メニュー名と使用する食材（タグ形式）をセットで保存できる。
- **献立の一覧・編集・削除**: 登録した献立を一覧表示し、編集・削除ができる。
- **食材逆引き検索**: 「にんじん」「豚肉」など食材名を入力すると、それを使う献立を絞り込み表示できる。
- **データ保存**: ローカルストレージを使用（ログイン不要で即使える）。→ **シーズン2でSupabaseへ移行済み**（7章）。「ログイン不要」の方針は現在も維持
- **UI方針**: モバイルファースト。スマホで片手操作しやすいレイアウト。

## 4. 技術スタック

おみくじアプリ（Next.js）の経験を活かし、同じ構成を採用します。

| 層 | 採用技術 | 備考 |
|---|---|---|
| フレームワーク | Next.js (React) / TypeScript | 現在 16.2.9。**学習データと差異があるため、AIにコードを書かせる前に `node_modules/next/dist/docs/` を読ませること**（`AGENTS.md` 参照） |
| スタイリング | Vanilla CSS | |
| データベース | **Supabase** | シーズン2で追加。`menus` / `api_usage` テーブル |
| AI | **Google Gemini API**（`gemini-flash-latest`） | 2026-08-11 追加。画像からの献立抽出に使用 |
| デプロイ | Vercel | おみくじアプリと同様 |

### 環境変数

| 変数名 | 用途 | 注意 |
|---|---|---|
| `NEXT_PUBLIC_SUPABASE_URL` | Supabase接続先 | ブラウザに露出する（設計上の前提） |
| `NEXT_PUBLIC_SUPABASE_ANON_KEY` | Supabase公開鍵 | 同上 |
| `GEMINI_API_KEY` | Gemini API | **`NEXT_PUBLIC_` を絶対に付けない。** 付けるとブラウザに露出し、第三者に課金される |

いずれもVercelの管理画面にも登録が必要（忘れると本番だけ動かない）。

## 5. 将来の拡張候補（今は作らない）
- 料理工程（レシピ手順）の追記機能
- ランダム1週間献立ルーレット
- 買い物リスト自動生成
- ~~夫婦間データ共有（DB連携）~~ → **シーズン2で実装済み**（7章）

## 6. 現在のステップ（進行予定）
1. [x] **要件定義**: 本指針の作成・承認
2. [x] **環境構築**: Next.js のプロジェクトを作成
3. [x] **MVP開発**: 献立登録・一覧・逆引き検索機能の実装
4. [x] **デプロイ**: Vercelを使ってWebに公開し、スマホからアクセス確認

## 7. シーズン2（夫婦間データ共有・クラウド化）
1. [x] **DB構築**: Supabaseプロジェクトの作成とキー取得
2. [x] **コード改修**: ローカルストレージからSupabase通信への書き換え
3. [-] **セキュリティ**: RLS（行レベルセキュリティ）の設定 → **見送り（2026-08-14 決定）**
4. [x] **再デプロイ**: 環境変数をVercelに設定して本番反映

### RLS見送りの判断（2026-08-14）

**結論：このアプリではRLSを設定しない。未着手ではなく、やらないと決めた。**

理由：

1. **認証機能を持たない設計のため、RLS単独では実効性がない。** RLSは「誰がアクセスしているか」を識別できて初めて機能する。ログインを持たない本アプリでは、RLSを足しても守れるものがない。実質的な判断は「認証を入れない」であり、RLSはその帰結
2. **守るべき情報がない。** 扱うのは献立名と食材のみ。個人情報・家計情報・認証情報を一切含まない
3. **金銭被害の経路がない。** Gemini APIは支払い方法未登録で運用しており、無料枠を超えれば停止するだけ
4. **データ消失を許容している。** `menus` のデータは失っても再登録可能な範囲

想定される最大の被害は「献立データが消える」「Gemini無料枠を他人に消費される」まで。いずれも復旧可能または一時的。

> ⚠️ **この判断の再検討が必要になる条件**
> - 個人情報・家計情報・健康情報を扱う機能を追加するとき
> - 夫婦以外の第三者にアプリを公開するとき
> - Gemini APIに支払い方法を登録するとき
>
> 上記のいずれかに該当したら、**認証の導入とセットで**RLSを再検討すること。

※旧記載の「守りの三柱」という表現は、三本柱の内訳がドキュメント内のどこにも定義されておらず、由来も不明だったため削除した（2026-08-14）。

---

## 8. データの保存形式（2026-08-02 制定）

食材は `menus.ingredients`（文字列の配列）に、次の形式で1件ずつ保存する。

```
豚肉:200g        通常の食材
#醤油:大さじ1     調味料（先頭の # がマーカー）
玉ねぎ           量なしも可
```

- `#` が付いた食材は**調味料**として扱い、買い物リストで**数量を集計しない**
- `#` が無い既存データはすべて通常食材として動く（後方互換）
- 「量」の部分は自由入力。書式が違っても壊れない設計にしてある

## 9. 量の書き方ルール

買い物リストの合計を正しく出すために、次の形式を推奨する。

| 項目 | ルール | 例 |
|---|---|---|
| 単位 | `g` / `ml` / `個` / `本` / `枚` / `パック` に揃える | `200g` |
| 分数 | 使わず小数で書く | `0.5個`（`1/2個` でも動く） |
| 全角 | 使わない（全角でも自動変換される） | `200g` |
| 調味料 | 量は任意。`大さじ1` `少々` のままでよい | `#醤油:大さじ1` |

### 集計の仕様

- **同じ単位のものだけを合計する**。単位が違えば `200g ＋ 1パック` と併記する
- 数値が取り出せない量（`大さじ1` `少々` `適量`）は**合計せず、重複を除いてそのまま並べる**
- `半分` `2分の1` `1と1/2` `½` `200グラム` `100cc` などは自動で解釈・換算する
- 調味料は名前だけを重複排除して、買い物リストの別セクションに表示する

## 10. 調味料の判定ルール

チェックが入る経路は3つ。いずれも**手で外せる**。

1. **組み込み辞書** — 醤油・みりん・砂糖・塩・油・だし・ルー類など約70語（`SEASONINGS`）
2. **学習** — 過去に一度でも調味料として登録した食材名は、次回から自動でON。Supabaseのデータを見るので**夫婦で共有される**
3. **一括登録の `#`** — AIが付けたマーカーをそのまま解釈する

### 注意点

- **既存データには遡って適用されない**。登録済みメニューに調味料フラグを付けるには、登録し直すか手動で編集する
- 「ごま油またはサラダ油」のような複合表記は辞書に一致しない。**1食材1行**で書くこと

## 11. 一括登録の運用

一括登録が主な登録経路。**経路は2つあり、A（画像から自動）が標準**。

### A. 画像から自動抽出（2026-08-11 実装・標準の経路）

アプリ内でGemini APIを呼ぶため、外部AIとの往復が不要。

1. アプリで**画像を選択**（サムネイルが表示される）
2. 自動抽出を実行 → Geminiが料理名・食材・分量を読み取る
3. **抽出結果を画面上で確認・修正**してから登録

**確認画面は必ず通す設計にしてある。** AIの読み取りは必ず誤るため、そのままDBへ入れない。

**1日20回の上限**（`DAILY_LIMIT`）を自前で設けている。`api_usage` テーブルで日別に回数を記録し、超過すると案内を出して停止する。Gemini無料枠を使い切らないための防御であり、**支払い方法を登録していないので課金は発生しない**。

#### 複数画像の同時添付（2026-08-14 実装）

1つのレシピが複数の画像に分割されている場合に対応する。

- **最大4枚**（`MAX_IMAGES`）。「＋追加」で1枚ずつ足せる。サムネイルの✕で個別に取り消せる
- **必ず上から順に選ぶこと**。選んだ順＝AIに渡す順で、レシピの続き順として解釈される
- **2枚以上のときは「1品の料理」として読み取り、必ず1行にまとめて返す**。複数のメニューには分割しない
- 1枚のときは従来どおり（画像内に複数メニューがあれば複数行で返る）
- **1回の抽出＝Gemini呼び出し1回**。4枚まとめても消費は1回分
- 合計サイズは**4MBまで**（`MAX_TOTAL_BYTES`）。Vercelのリクエストボディ上限4.5MBの手前で止めている。超過時はエラーを出して送信しない

**既知の制約**：1つのページに主菜と副菜が載っているような画像を複数枚渡すと、2品が1行に混ざる。確認画面で修正すること。頻発するようなら仕様を見直す。

**並べ替えUIは作っていない**。順番を間違えたら✕で消して選び直す。

### B. 手動コピペ（従来方式・代替経路として維持）

Aの上限に達した日や、Aがうまく動かないときの逃げ道として残してある。**削除しないこと。**

1. アプリの「呪文をコピーする」で**最新のプロンプトを取得**（更新されることがあるので毎回コピーし直す）
2. レシピ画像とともにAIへ渡す
3. 返ってきたテキストを貼り付けて登録

### 共通の注意

一括登録は**新規追加**なので、同じメニューを登録し直すときは**先に既存データを削除する**こと（重複する）。

### 運用実績（2026-08-14 時点）

| 項目 | 状況 |
|---|---|
| 画質 | 問題なし。Webページの縮小スクリーンショットが主で、読み取りに支障は出ていない |
| 1日20回の上限 | 十分足りている。引き上げの必要なし |
| 複数画像 | 2026-08-14に最大4枚の同時添付を実装（上記参照）。1枚ずつしか貼れなかった問題は解消 |

### 呪文には登録済みの食材名が自動で付く

「呪文をコピーする」を押すと、末尾に**登録済みの食材名一覧（最大200件・使用回数の多い順）**が自動で追記される。AIに我が家の表記見本を渡すことで、出力が既存の表記に揃う。調味料は `#醤油` の形で入るので、調味料判定も同時に揃う。

一覧は登録のたびに自動更新されるのでメンテ不要。ただし**毎回コピーし直さないと最新にならない**。

## 12. 表記ゆれ対策

対策は4段階で考える。上から順に費用対効果が高い。

| # | 手段 | 状態 |
|---|---|---|
| 1 | **入力サジェスト**（手入力の候補表示＋呪文への既存名差し込み） | ✅ 2026-08-02 実装 |
| 2 | 集計キーの正規化（全角半角・カタカナ→ひらがな） | 未着手 |
| 3 | 別名辞書（`ingredient_aliases` テーブル。人が「同じ」と決める） | 未着手 |
| 4 | 週1のAIバッチ統一 | 保留 |

**方針**：ゆれを直すより、発生させないほうが安い。1と2で入口と集計を固め、残る「豚肉 と 豚バラ肉 は同じか」のような判断は人が決める（3）。AIによる全自動統合（4）は誤統合が見えないので最後に回す。

**やらないこと**：形態素解析（kuromoji.js）で漢字の読みを取る案。バンドルが数MB増えてスマホで重くなる上、「豚肉」と「豚バラ肉」の判定はできない。

## 13. その他の未着手課題

| 課題 | 内容 |
|---|---|
| CSV入出力 | 登録データをCSVで出し入れし、週1でAIに表記統一させる運用 |
| 貼り付け時の補正確認 | 一括登録の貼り付け後に「たまねぎ → 玉ねぎ に直しますか？」と確認する画面 |

※RLSは「未着手課題」から除外した。やらないと決めた項目のため（詳細は7章）。

---

## 改訂履歴

| 日付 | 内容 |
|---|---|
| 2026-08-02 | 量パーサの再実装、調味料フラグ（`#`）、買い物リストの調味料分離、一括登録プロンプトの強化。協議記録は `20_Project/Menu_App_Docs/2026-08-02_献立アプリ改善協議_議事録.md` |
| 2026-08-02 | 表記ゆれ対策の第1段：食材名の入力サジェスト（datalist）と、呪文への登録済み食材名の自動差し込み |
| 2026-08-11 | **Gemini API連携**：アプリ内で画像から献立を自動抽出する機能を追加（`src/app/api/extract-menu/route.ts`）。外部AIとの手動往復を解消。1日20回の利用上限（`api_usage` テーブル）を実装。設計計画は `AI_INTEGRATION_PLAN.md` |
| 2026-08-14 | **複数画像の同時添付**：最大4枚を1回の抽出でGeminiに渡せるようにした。2枚以上は「1つのレシピの分割」として1行にまとめて返す（11章A）。13章の課題から削除 |
| 2026-08-14 | RLSを「見送り」と決定（7章に判断理由と再検討条件を追記）。定義不明だった「守りの三柱」の表現を削除。4章に技術スタックと環境変数、11章に画像自動抽出の運用と実績を追記 |
